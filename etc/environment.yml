name: svdimpute
channels:
  - conda-forge
  - nodefaults
dependencies:
  # required
  - python >= 3.10
  - numpy>=1.20.0
  - pandas>=1.3.0
  - scikit-learn>=1.0.0
  # optional
  - matplotlib
  - pip
  - jupyter
  - jupytext
  - jupyterlab
  # tests
#  - coveralls
  - flaky
  - pytest
  - pytest-cov
  - pytest-xdist
  - pytest-timeout
  - pip:
    - -e ..


